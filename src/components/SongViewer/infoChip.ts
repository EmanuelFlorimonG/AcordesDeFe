/**
 * Shared look of the small musical-information chips in the song header.
 * They sit on the coloured header banner, whose colour depends on the
 * category. A faint dark tint (rather than a white one) keeps white text
 * readable on the light banners too, such as Gloria's yellow.
 */
export const BANNER_CHIP_CLASS =
  'inline-flex items-center h-8 rounded-lg bg-slate-950/15 border border-white/25 text-xs text-white backdrop-blur-sm';

export const BANNER_CHIP_LABEL_CLASS = 'text-white/80';

export const BANNER_CHIP_VALUE_CLASS = 'font-mono text-[13px] font-bold text-white';
